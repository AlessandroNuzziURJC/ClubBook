package clubbook.backend.model.enumClasses;

/**
 * Enum representing the different roles in the application.
 * This enumeration is used to define the access levels and permissions
 * associated with different types of users.
 */
public enum RoleEnum {
    STUDENT,
    TEACHER,
    ADMINISTRATOR;
}
