package clubbook.backend.model.enumClasses;

/**
 * Enum representing different types of events in the application.
 * This enumeration is used to categorize events based on their nature.
 */
public enum EventTypeEnum {
    COMPETITION,
    EXHIBITION,
    TRAINING
}
